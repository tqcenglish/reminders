import 'package:flutter/widgets.dart';

class ModelMenuItem {
  String name;
  Color color;

  ModelMenuItem({
      String name,
      Color color
      }) {
    this.name = name;
    this.color= color;
  }
}
